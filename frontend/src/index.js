import React from 'react';
import { render } from 'react-dom';
import 'bootstrap'
import App from './App';
import './index.css';

const root = document.getElementById('root');

render(<App />, root);
